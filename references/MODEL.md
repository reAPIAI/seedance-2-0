# Seedance 2.0 public reAPI notes

Skill name: `seedance-2-0`

Default canonical model ID:

```text
doubao-seedance-2.0
```

Endpoint kind:

```text
videos
```

Supported model IDs:

- `doubao-seedance-2.0`
- `doubao-seedance-2.0-fast`
- `doubao-seedance-2.0-face`
- `doubao-seedance-2.0-fast-face`

Common public parameters:

- Default canonical model ID: `doubao-seedance-2.0`.
- Fast and face-focused variants are documented as `doubao-seedance-2.0-fast`, `doubao-seedance-2.0-face`, and `doubao-seedance-2.0-fast-face`.
- Common fields: `prompt`, `duration`, `size`, `resolution`, `seed`, `generate_audio`, `image_urls`, `video_urls`, `audio_urls`.

Public docs:

```text
https://reapi.ai/docs/seedance-2-0
```

Use only documented reAPI request fields. Do not add private implementation
notes, private repository paths, deployment details, database details, or API
keys to this skill.
